import { Box, Typography } from "@material-ui/core"
import React from "react"

const Header = () => {
  return (
    <Box py={5}>
      <Typography variant="h4">Image Repo Client</Typography>
    </Box>
  )
}

export default Header
